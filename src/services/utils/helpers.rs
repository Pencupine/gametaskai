// use std::env;
// use std::path::PathBuf;

// pub fn fetch_current_directory() -> Result<std::path::PathBuf, std::io::Error> {
//     let current_dir = env::current_dir()?;
//     Ok(current_dir)
// }

// pub fn fetch_electron_directory() {
//     let current_dir = env::current_dir().expect("Failed to fetch current directory");
//     println!("Current directory: {:?}", current_dir);

//     let relative_path = PathBuf::from("electron/");
//     let absolute_path = current_dir.join(relative_path);
//     return 
// }

